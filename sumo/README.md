# SUMO extension

This folder is an optional SUMO/TraCI scaffold.

The Vercel web demo does not run SUMO. For a full SUMO experiment, create a valid SUMO network with `netconvert` or SUMO GUI and then connect it to a Python TraCI/RL controller.

The browser demo remains independent and deployable on Vercel.
